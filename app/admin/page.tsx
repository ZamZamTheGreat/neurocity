"use client";
import { FormEvent, useEffect, useState } from "react";
type Application = { id: number; reference: string; status: string; tradingName: string; legalName: string; category: string; representativeName: string; email: string; phone: string; description: string; submittedAt: string };
export default function AdminPage() { const [items, setItems] = useState<Application[] | null>(null); const [auth, setAuth] = useState({ name: "Sergej Witbooi", email: "sergejwitbooi@gmail.com", password: "" }); const [mode, setMode] = useState<"login" | "register">("register"); const [message, setMessage] = useState(""); async function load() { const response = await fetch("/api/admin/applications"); setItems(response.ok ? (await response.json()).applications : null); } useEffect(() => { load(); }, []); async function authenticate(event: FormEvent) { event.preventDefault(); const response = await fetch(`/api/auth/${mode}`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(auth) }); const data = await response.json(); if (!response.ok) return setMessage(data.error); setMessage(""); await load(); } async function review(id: number, status: string) { const notes = window.prompt("Optional review note") ?? ""; const response = await fetch("/api/admin/applications", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ id, status, notes }) }); const data = await response.json(); setMessage(response.ok ? `Application moved to ${status.replaceAll("_", " ")}.` : data.error); if (response.ok) await load(); }
  if (!items) return <main className="admin-auth"><a href="/" className="brand"><span>Neuro</span><strong>City</strong></a><form onSubmit={authenticate}><p className="eyebrow">NeuroCity administration</p><h1>{mode === "register" ? "Create the first administrator" : "Administrator login"}</h1>{mode === "register" && <label>Full name<input value={auth.name} onChange={(e) => setAuth({ ...auth, name: e.target.value })} /></label>}<label>Email<input type="email" value={auth.email} onChange={(e) => setAuth({ ...auth, email: e.target.value })} /></label><label>Password<input type="password" minLength={10} value={auth.password} onChange={(e) => setAuth({ ...auth, password: e.target.value })} /></label>{message && <p className="form-error">{message}</p>}<button>{mode === "register" ? "Create administrator" : "Sign in"}</button><button type="button" className="switch-auth" onClick={() => setMode(mode === "register" ? "login" : "register")}>{mode === "register" ? "I already have an account" : "Create the first administrator"}</button></form></main>;
  return <main className="admin-page"><header><div><p className="eyebrow">NeuroCity operations</p><h1>Merchant applications</h1></div><a href="/">View mall</a></header>{message && <button className="workspace-message" onClick={() => setMessage("")}>{message} ×</button>}<section className="admin-applications">{items.length === 0 ? <div className="empty-state"><h2>No applications yet</h2></div> : items.map((item) => <article key={item.id}><header><div><small>{item.reference}</small><h2>{item.tradingName}</h2><p>{item.legalName} · {item.category}</p></div><span>{item.status.replaceAll("_", " ")}</span></header><p>{item.description}</p><dl><div><dt>Representative</dt><dd>{item.representativeName}</dd></div><div><dt>Contact</dt><dd>{item.email}<br />{item.phone}</dd></div><div><dt>Submitted</dt><dd>{new Date(item.submittedAt).toLocaleDateString("en-NA")}</dd></div></dl><footer><button onClick={() => review(item.id, "under_review")}>Start review</button><button onClick={() => review(item.id, "more_information_required")}>Request information</button><button className="approve" onClick={() => review(item.id, "approved")}>Approve</button><button className="reject" onClick={() => review(item.id, "rejected")}>Reject</button></footer></article>)}</section></main>;
}
