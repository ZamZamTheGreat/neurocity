import { eq } from "drizzle-orm";
import { getDb } from "../../../../db";
import { auditEvents, merchants } from "../../../../db/schema";
import { requirePilotMerchant } from "../auth";
export async function GET() { const access = await requirePilotMerchant(); if (!access) return Response.json({ error: "Merchant access required." }, { status: 403 }); const [merchant] = await getDb().select().from(merchants).where(eq(merchants.id, access.merchantId)).limit(1); return Response.json({ merchant, role: access.membership.role }); }
export async function PATCH(request: Request) { const access = await requirePilotMerchant(["owner", "manager"]); if (!access) return Response.json({ error: "Owner or manager access required." }, { status: 403 }); const payload = await request.json() as { name?: string; category?: string; contactName?: string; contactEmail?: string; contactPhone?: string; website?: string; pickupLocation?: string; deliveryMode?: string }; const name = payload.name?.trim(); const email = payload.contactEmail?.trim(); if (!name || !email?.includes("@") || !payload.pickupLocation?.trim()) return Response.json({ error: "Business name, contact email and pickup location are required." }, { status: 400 }); const values = { name, category: payload.category?.trim() || "Other", contactName: payload.contactName?.trim() || null, contactEmail: email, contactPhone: payload.contactPhone?.trim() || null, website: payload.website?.trim() || null, pickupLocation: payload.pickupLocation.trim(), deliveryMode: payload.deliveryMode === "platform_managed" ? "platform_managed" : "merchant_managed", setupStep: 3 }; const db = getDb(); const [merchant] = await db.update(merchants).set(values).where(eq(merchants.id, access.merchantId)).returning(); await db.insert(auditEvents).values({ actorRef: access.user.userId, action: "merchant.setup_updated", resourceType: "merchant", resourceId: String(access.merchantId), metadata: JSON.stringify(values), createdAt: new Date() }); return Response.json({ merchant }); }
