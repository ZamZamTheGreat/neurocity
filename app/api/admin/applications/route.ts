import { and, desc, eq } from "drizzle-orm";
import { getDb } from "../../../../db";
import { auditEvents, merchantApplications, merchants } from "../../../../db/schema";
import { getChatGPTUser } from "../../../chatgpt-auth";
const allowed = new Set(["under_review", "more_information_required", "approved", "rejected"]);
const slugify = (value: string) => value.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "").slice(0, 180);
export async function GET() { const user = await getChatGPTUser(); if (user?.platformRole !== "administrator") return Response.json({ error: "Administrator access required." }, { status: 403 }); return Response.json({ applications: await getDb().select().from(merchantApplications).orderBy(desc(merchantApplications.submittedAt)) }); }
export async function PATCH(request: Request) { const user = await getChatGPTUser(); if (user?.platformRole !== "administrator") return Response.json({ error: "Administrator access required." }, { status: 403 }); const { id, status, notes } = await request.json() as { id?: number; status?: string; notes?: string }; if (!Number.isInteger(id) || !status || !allowed.has(status)) return Response.json({ error: "Valid application and review status required." }, { status: 400 }); const db = getDb(); const [application] = await db.select().from(merchantApplications).where(eq(merchantApplications.id, id!)).limit(1); if (!application) return Response.json({ error: "Application not found." }, { status: 404 }); let merchantId = application.merchantId; await db.transaction(async (tx) => { if (status === "approved" && !merchantId) { const [merchant] = await tx.insert(merchants).values({ name: application.tradingName, slug: `${slugify(application.tradingName) || "merchant"}-${application.id}`, category: application.category, status: "onboarding", contactName: application.representativeName, contactEmail: application.email, contactPhone: application.phone, website: application.website, pickupLocation: application.branchLocations, deliveryMode: application.deliveryAvailable ? "merchant_managed" : "pickup_only", setupStep: 1 }).returning(); merchantId = merchant.id; } await tx.update(merchantApplications).set({ status, reviewNotes: notes?.trim() || null, reviewedAt: new Date(), reviewedBy: Number(user.userId), merchantId }).where(and(eq(merchantApplications.id, application.id), eq(merchantApplications.status, application.status))); await tx.insert(auditEvents).values({ actorRef: user.userId, action: `application.${status}`, resourceType: "merchant_application", resourceId: String(application.id), metadata: { notes: notes?.trim() || null, merchantId } }); }); return Response.json({ application: { ...application, status, reviewNotes: notes, merchantId } }); }
